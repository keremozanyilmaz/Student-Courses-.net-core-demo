@model List<KUSYS_Demo.Models.StudentModel>
@{
    ViewData["Title"] = "GetAll";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Student and Courses</h1>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("First Name")
            </th>
            <th>
                @Html.DisplayName("Last Name")
            </th>
            <th>
                @Html.DisplayName("Course Name")
            </th>
           @* <th></th>*@
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseName)
            </td>
        @*    <td>
                <a asp-action="Edit" asp-route-id="@item.StudentId">Edit</a> |
                <a itemid="details" data-toggle="modal" data-target="#centermodal" type="submit" onclick="showInPopup('@Url.Action("Details","Student", new {id= item.StudentId})','Details')" asp-action="Details" asp-route-id="@item.StudentId">Details</a> |
                <a  asp-action="Delete" asp-route-id="@item.StudentId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
